services:
  - type: web
    name: medbill-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    branch: main
    numInstances: 1
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 8080
